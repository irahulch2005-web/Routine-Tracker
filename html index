<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Routine OS</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@500;600;700&family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500;600;700&display=swap" rel="stylesheet">
<link rel="stylesheet" href="styles.css">
</head>
<body>

<div class="app">

  <!-- SIDEBAR -->
  <aside class="sidebar" id="sidebar">
    <div class="sidebar-top">
      <div class="wordmark"><span class="wordmark-dot"></span>Routine<span class="wordmark-light">OS</span></div>
      <button class="sidebar-close" id="sidebarClose" aria-label="Close menu">✕</button>
    </div>

    <nav class="nav" id="nav">
      <button class="nav-item is-active" data-section="dashboard"><span class="nav-ic">◆</span>Dashboard</button>
      <button class="nav-item" data-section="steps"><span class="nav-ic">◈</span>Steps</button>
      <button class="nav-item" data-section="gym"><span class="nav-ic">▲</span>Gym</button>
      <button class="nav-item" data-section="coding"><span class="nav-ic">◧</span>Coding</button>
      <button class="nav-item" data-section="study"><span class="nav-ic">▣</span>Semester Study</button>
      <button class="nav-item" data-section="genalpha"><span class="nav-ic">●</span>Gen Alpha</button>
      <button class="nav-item" data-section="memestiano"><span class="nav-ic">■</span>Memestiano</button>
      <button class="nav-item" data-section="reading"><span class="nav-ic">▤</span>Reading</button>
      <button class="nav-item" data-section="entertainment"><span class="nav-ic">◐</span>Entertainment</button>
      <button class="nav-item" data-section="calendar"><span class="nav-ic">▦</span>Week</button>
      <button class="nav-item" data-section="review"><span class="nav-ic">◎</span>Weekly Review</button>
      <button class="nav-item" data-section="settings"><span class="nav-ic">⚙</span>Settings</button>
    </nav>

    <div class="sidebar-bottom">
      <div class="daytype-toggle" id="daytypeToggleSidebar">
        <button class="daytype-btn is-active" data-type="college">College Day</button>
        <button class="daytype-btn" data-type="nonCollege">Non-College Day</button>
      </div>
      <div class="sidebar-date" id="sidebarDate">—</div>
    </div>
  </aside>

  <div class="sidebar-scrim" id="sidebarScrim"></div>

  <!-- MAIN -->
  <main class="main">

    <header class="topbar">
      <button class="hamburger" id="hamburger" aria-label="Open menu">☰</button>
      <div class="topbar-title" id="topbarTitle">Dashboard</div>
      <div class="topbar-actions">
        <button class="icon-btn" id="exportBtn" title="Export data">⇩</button>
        <button class="icon-btn" id="importBtn" title="Import data">⇧</button>
        <input type="file" id="importFile" accept="application/json" hidden>
      </div>
    </header>

    <div class="content">

      <!-- ============ DASHBOARD ============ -->
      <section class="view is-active" id="view-dashboard">

        <div class="rightnow-card">
          <div class="rightnow-top">
            <div class="rightnow-date" id="rnDate">Friday, Aug 14</div>
            <div class="daytype-toggle daytype-toggle--inline" id="daytypeToggleMain">
              <button class="daytype-btn is-active" data-type="college">College Day</button>
              <button class="daytype-btn" data-type="nonCollege">Non-College Day</button>
            </div>
          </div>

          <div class="rightnow-main">
            <div class="rightnow-block">
              <div class="rightnow-label">Right now</div>
              <div class="rightnow-activity" id="rnActivity">—</div>
              <div class="rightnow-sub" id="rnSub">—</div>
            </div>
            <div class="rightnow-next">
              <div class="rightnow-label">Next up</div>
              <div class="rightnow-next-activity" id="rnNextActivity">—</div>
              <div class="rightnow-next-time" id="rnNextTime">—</div>
            </div>
          </div>

          <div class="timeline" id="timeline">
            <div class="timeline-track" id="timelineTrack"></div>
            <div class="timeline-marker" id="timelineMarker"></div>
          </div>
        </div>

        <div class="dash-grid">
          <div class="dash-main">
            <div class="section-head">
              <h2>Today's habits</h2>
              <div class="today-progress" id="todayProgress">0% today</div>
            </div>
            <div class="habit-grid" id="habitGrid"></div>
          </div>

          <div class="dash-side">
            <div class="card steps-mini" id="stepsMiniCard">
              <div class="card-label">Steps</div>
              <div class="ring-wrap">
                <svg viewBox="0 0 120 120" class="ring">
                  <circle cx="60" cy="60" r="52" class="ring-bg"/>
                  <circle cx="60" cy="60" r="52" class="ring-fg" id="stepsRing"/>
                </svg>
                <div class="ring-center">
                  <div class="ring-value" id="stepsValue">0</div>
                  <div class="ring-target">/ 5,000</div>
                </div>
              </div>
              <div class="steps-quickadd">
                <button data-add="1000">+1,000</button>
                <button data-add="2500">+2,500</button>
                <button data-add="5000">Set 5,000</button>
              </div>
            </div>

            <div class="card">
              <div class="card-label">Consistency score</div>
              <div class="score-big" id="scoreBig">0%</div>
              <div class="score-bars" id="scoreBars"></div>
            </div>

            <div class="card">
              <div class="card-label">This week</div>
              <div class="weeksnap" id="weekSnap"></div>
            </div>
          </div>
        </div>
      </section>

      <!-- ============ STEPS ============ -->
      <section class="view" id="view-steps">
        <div class="section-head"><h2>Steps</h2></div>
        <div class="card steps-full">
          <div class="ring-wrap ring-wrap--lg">
            <svg viewBox="0 0 160 160" class="ring">
              <circle cx="80" cy="80" r="70" class="ring-bg"/>
              <circle cx="80" cy="80" r="70" class="ring-fg" id="stepsRingFull"/>
            </svg>
            <div class="ring-center">
              <div class="ring-value ring-value--lg" id="stepsValueFull">0</div>
              <div class="ring-target">/ 5,000 steps</div>
            </div>
          </div>
          <p class="hint">5,000 steps is roughly 3.5–4 km, depending on stride length. College, gym, and normal movement all count — you don't need to walk it all at night.</p>
          <div class="manual-entry">
            <input type="number" id="stepsManualInput" placeholder="Enter total steps today" min="0">
            <button id="stepsManualSet" class="btn btn--primary">Set</button>
          </div>
          <div class="steps-quickadd steps-quickadd--lg">
            <button data-add="500">+500</button>
            <button data-add="1000">+1,000</button>
            <button data-add="2500">+2,500</button>
            <button id="stepsReset" class="btn--ghost">Reset</button>
          </div>
        </div>
        <div class="card">
          <div class="card-label">Last 7 days</div>
          <div class="bar-row" id="stepsHistory"></div>
        </div>
      </section>

      <!-- ============ GYM ============ -->
      <section class="view" id="view-gym">
        <div class="section-head"><h2>Gym</h2></div>
        <div class="stat-row">
          <div class="stat-card"><div class="stat-num" id="gymWeekCount">0/7</div><div class="stat-label">This week</div></div>
          <div class="stat-card"><div class="stat-num" id="gymStreak">0</div><div class="stat-label">Current streak</div></div>
        </div>
        <div class="card">
          <div class="card-label">Today</div>
          <div class="status-btns" id="gymStatusBtns"></div>
          <textarea id="gymNotes" class="notes-input" placeholder="Optional workout notes — what you trained, how it felt..."></textarea>
        </div>
        <div class="card">
          <div class="card-label">Last 7 days</div>
          <div class="bar-row" id="gymHistory"></div>
        </div>
      </section>

      <!-- ============ CODING ============ -->
      <section class="view" id="view-coding">
        <div class="section-head"><h2>Coding</h2><div class="target-pill">Target: 4/week · min 20 min</div></div>
        <div class="card">
          <div class="card-label">Log today's session</div>
          <div class="status-btns" id="codingStatusBtns"></div>
          <div class="form-row">
            <input type="text" id="codingTopic" placeholder="Topic">
            <input type="number" id="codingDuration" placeholder="Minutes">
          </div>
          <div class="form-row">
            <input type="text" id="codingProject" placeholder="Project worked on">
          </div>
          <textarea id="codingLearned" class="notes-input" placeholder="What did you learn today?"></textarea>
          <button class="btn btn--primary" id="codingSave">Save session</button>
        </div>
        <div class="card">
          <div class="card-label">Recent sessions</div>
          <div class="log-list" id="codingLog"></div>
        </div>
      </section>

      <!-- ============ STUDY ============ -->
      <section class="view" id="view-study">
        <div class="section-head"><h2>Semester study</h2><button class="btn btn--small" id="addSubjectBtn">+ Add subject</button></div>
        <div class="subject-grid" id="subjectGrid"></div>
      </section>

      <!-- ============ GEN ALPHA ============ -->
      <section class="view" id="view-genalpha">
        <div class="section-head"><h2>Gen Alpha</h2><div class="target-pill" id="genAlphaTarget">0/3 this week</div></div>
        <div class="content-tasks" id="genAlphaTasks"></div>
        <div class="card">
          <div class="card-label">Add task</div>
          <div class="form-row">
            <select id="genAlphaStage">
              <option>Ideas</option><option>Research</option><option>Writing</option><option>Designing/editing</option><option>Posted</option>
            </select>
            <input type="text" id="genAlphaTaskText" placeholder="What's this task?">
          </div>
          <button class="btn btn--primary" id="genAlphaAdd">Add</button>
        </div>
      </section>

      <!-- ============ MEMESTIANO ============ -->
      <section class="view" id="view-memestiano">
        <div class="section-head"><h2>Memestiano</h2><div class="target-pill" id="memestianoTarget">0/2 this week</div></div>
        <div class="content-tasks" id="memestianoTasks"></div>
        <div class="card">
          <div class="card-label">Add task</div>
          <div class="form-row">
            <select id="memestianoStage">
              <option>Reel ideas</option><option>Meme ideas</option><option>Motivation</option><option>Political satire</option><option>Editing</option><option>Posted</option>
            </select>
            <input type="text" id="memestianoTaskText" placeholder="What's this task?">
          </div>
          <button class="btn btn--primary" id="memestianoAdd">Add</button>
        </div>
      </section>

      <!-- ============ READING ============ -->
      <section class="view" id="view-reading">
        <div class="section-head"><h2>Reading</h2><div class="target-pill">Daily minimum: 1 page</div></div>
        <div class="card" id="currentBookCard">
          <div class="card-label">Current book</div>
          <div class="form-row">
            <input type="text" id="bookTitle" placeholder="Book title">
            <input type="number" id="bookTotalPages" placeholder="Total pages">
          </div>
          <button class="btn btn--small" id="setCurrentBook">Set as current</button>
          <div class="current-book-display" id="currentBookDisplay"></div>
          <div class="form-row" id="pagesTodayRow" style="display:none">
            <input type="number" id="pagesToday" placeholder="Pages read today">
            <button class="btn btn--primary" id="logPagesBtn">Log</button>
          </div>
        </div>
        <div class="stat-row">
          <div class="stat-card"><div class="stat-num" id="readingStreak">0</div><div class="stat-label">Reading streak</div></div>
          <div class="stat-card"><div class="stat-num" id="booksCompleted">0</div><div class="stat-label">Books completed</div></div>
        </div>
        <div class="card">
          <div class="card-label">Completed books</div>
          <div class="log-list" id="booksLog"></div>
        </div>
      </section>

      <!-- ============ ENTERTAINMENT ============ -->
      <section class="view" id="view-entertainment">
        <div class="section-head"><h2>Entertainment</h2></div>
        <p class="hint">This time is intentional, not wasted. Logging it just keeps the balance visible.</p>
        <div class="card">
          <div class="form-row">
            <select id="entType">
              <option>Movie</option><option>Series</option><option>YouTube</option><option>Gaming</option><option>Friends/social</option><option>Other</option>
            </select>
            <input type="number" id="entMinutes" placeholder="Minutes">
          </div>
          <input type="text" id="entNotes" placeholder="Notes (optional)" class="full-input">
          <button class="btn btn--primary" id="entAdd">Log time</button>
        </div>
        <div class="stat-row">
          <div class="stat-card"><div class="stat-num" id="entWeekTotal">0h</div><div class="stat-label">This week</div></div>
        </div>
        <div class="card">
          <div class="card-label">Today's log</div>
          <div class="log-list" id="entLog"></div>
        </div>
      </section>

      <!-- ============ CALENDAR ============ -->
      <section class="view" id="view-calendar">
        <div class="section-head"><h2>This week</h2></div>
        <div class="week-grid" id="weekGrid"></div>
      </section>

      <!-- ============ WEEKLY REVIEW ============ -->
      <section class="view" id="view-review">
        <div class="section-head"><h2>Weekly review</h2></div>
        <div class="review-grid" id="reviewGrid"></div>
        <div class="card">
          <div class="card-label">Reflection</div>
          <label class="reflect-label">What went well?</label>
          <textarea class="notes-input" id="reflectGood"></textarea>
          <label class="reflect-label">What did you struggle with?</label>
          <textarea class="notes-input" id="reflectStruggle"></textarea>
          <label class="reflect-label">What should change next week?</label>
          <textarea class="notes-input" id="reflectChange"></textarea>
          <label class="reflect-label">The ONE habit to protect next week</label>
          <input type="text" id="reflectProtect" class="full-input">
          <button class="btn btn--primary" id="saveReflection">Save reflection</button>
        </div>
      </section>

      <!-- ============ SETTINGS ============ -->
      <section class="view" id="view-settings">
        <div class="section-head"><h2>Settings</h2></div>

        <div class="card">
          <div class="card-label">Schedule blocks</div>
          <div class="daytype-toggle daytype-toggle--inline" id="settingsRoutinePicker">
            <button class="daytype-btn is-active" data-type="college">College Day</button>
            <button class="daytype-btn" data-type="nonCollege">Non-College Day</button>
          </div>
          <div class="schedule-editor" id="scheduleEditor"></div>
          <button class="btn btn--small" id="addBlockBtn">+ Add block</button>
        </div>

        <div class="card">
          <div class="card-label">Weekly targets</div>
          <div class="targets-editor" id="targetsEditor"></div>
        </div>

        <div class="card">
          <div class="card-label">Step target</div>
          <div class="form-row">
            <input type="number" id="settingsStepTarget" placeholder="5000">
          </div>
        </div>

        <div class="card">
          <div class="card-label">Data</div>
          <div class="settings-data-row">
            <button class="btn btn--small" id="exportBtn2">Export JSON backup</button>
            <button class="btn btn--small" id="importBtn2">Import JSON backup</button>
            <button class="btn btn--ghost-danger" id="resetAllBtn">Reset all data</button>
          </div>
        </div>
      </section>

    </div>
  </main>
</div>

<div class="toast" id="toast"></div>

<script src="app.js"></script>
</body>
</html>
